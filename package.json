{
  "name": "line-differential-relay-lab",
  "version": "0.8.0",
  "private": true,
  "description": "Industrial real-time 87L laboratory with packet-driven anomalies, blind waveform tracking, failure-driven hardening, Monte Carlo validation, and accelerated long-horizon stress testing.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/worker/simulation-worker.js && node --check src/engine/simulation.js && node --check src/validation/monte-carlo.js && node --check src/validation/long-horizon-stress.js && node --check src/validation/recovery-qualified-stress.js && node --check scripts/monte-carlo.mjs && node --check scripts/long-horizon-stress.mjs",
    "benchmark": "node scripts/monte-carlo.mjs",
    "benchmark:smoke": "node scripts/monte-carlo.mjs --smoke --compact --output artifacts/validation",
    "benchmark:stress": "node scripts/long-horizon-stress.mjs",
    "benchmark:stress:smoke": "node scripts/long-horizon-stress.mjs --smoke --compact --require-baseline-failures --output artifacts/long-horizon-stress",
    "validate": "npm run check && npm test && npm run benchmark:smoke && npm run benchmark:stress:smoke && npm run build"
  },
  "keywords": [
    "87l",
    "line-differential",
    "protection-relay",
    "power-system-protection",
    "relay-simulation",
    "waveform-tracking",
    "communication-delay",
    "monte-carlo-validation",
    "rare-event-stress",
    "github-pages"
  ],
  "author": "Ari / SonkuPik Engineering",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/masarray/line-differential-relay-lab.git"
  },
  "homepage": "https://masarray.github.io/line-differential-relay-lab/",
  "bugs": {
    "url": "https://github.com/masarray/line-differential-relay-lab/issues"
  },
  "packageManager": "npm@10.9.2"
}
