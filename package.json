{
  "name": "line-differential-relay-lab",
  "version": "0.10.1",
  "private": true,
  "description": "Deterministic 87L research laboratory with packet-driven communication stress, bounded waveform-assisted alignment, runtime safety invariants, and evidence-qualified dependability validation.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/worker/simulation-worker.js && node --check src/engine/simulation.js && node --check src/engine/safety-invariants.js && node --check src/ui/presentation-waveform.js && node --check src/validation/monte-carlo.js && node --check src/validation/long-horizon-stress.js && node --check src/validation/recovery-qualified-stress.js && node --check src/validation/degraded-dependability.js && node --check src/validation/qualified-operating-timing.js && node --check src/validation/evidence-qualified-dependability.js && node --check src/validation/reliability-freeze.js && node --check scripts/monte-carlo.mjs && node --check scripts/long-horizon-stress.mjs && node --check scripts/degraded-dependability.mjs && node --check scripts/reliability-freeze.mjs && node --check scripts/build.mjs",
    "benchmark": "node scripts/monte-carlo.mjs",
    "benchmark:smoke": "node scripts/monte-carlo.mjs --smoke --compact --output artifacts/validation",
    "benchmark:stress": "node scripts/long-horizon-stress.mjs",
    "benchmark:stress:smoke": "node scripts/long-horizon-stress.mjs --smoke --compact --require-baseline-failures --output artifacts/long-horizon-stress",
    "benchmark:dependability": "node scripts/degraded-dependability.mjs",
    "benchmark:dependability:smoke": "node scripts/degraded-dependability.mjs --smoke --compact --require-no-eligible-misses --require-eligible-cases --output artifacts/degraded-dependability",
    "benchmark:reliability": "node scripts/reliability-freeze.mjs",
    "validate": "npm run check && npm test && npm run benchmark:smoke && npm run benchmark:stress:smoke && npm run benchmark:dependability:smoke && npm run benchmark:reliability && npm run build"
  },
  "keywords": [
    "87l",
    "line-differential",
    "protection-relay",
    "power-system-protection",
    "relay-simulation",
    "waveform-tracking",
    "degraded-operation",
    "communication-delay",
    "monte-carlo-validation",
    "rare-event-stress",
    "safety-invariants",
    "github-pages"
  ],
  "author": "Ari / SonkuPik Engineering",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/masarray/line-differential-relay-lab.git"
  },
  "homepage": "https://masarray.github.io/line-differential-relay-lab/",
  "bugs": {
    "url": "https://github.com/masarray/line-differential-relay-lab/issues"
  },
  "packageManager": "npm@10.9.2"
}
